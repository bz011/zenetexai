import type { Metadata } from "next";
import ToolsContent from "./ToolsContent";

export const metadata: Metadata = { title: "Tools — ZENETEXAI" };

export default function ToolsPage() {
  return <ToolsContent />;
}
